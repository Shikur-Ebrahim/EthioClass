name: frontend
description: "A new Flutter project for EthioClass."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  go_router: ^13.2.0
  dio: ^5.4.1
  supabase_flutter: ^2.3.4
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.6
  video_player: ^2.8.3
  chewie: ^1.7.4
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
